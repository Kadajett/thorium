{
  "name": "thorium",
  "version": "0.1.0",
  "private": true,
  "description": "Dual-screen game platform for AYN Thor",
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "typecheck": "pnpm -r --if-present typecheck && pnpm -r --if-present check",
    "test": "pnpm -r --if-present test",
    "test:android-game-gate": "node --test apps/android/scripts/frame-present.test.mjs",
    "check:rust": "cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace",
    "check:android": "cd apps/android && ./gradlew :app:testDebugUnitTest :app:lintDebug :app:assembleDebug",
    "check": "pnpm typecheck && pnpm test && pnpm build && pnpm check:rust"
  }
}
